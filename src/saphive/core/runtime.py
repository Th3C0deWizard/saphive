"""Core runtime facade for SAPHive."""

from dataclasses import dataclass


@dataclass(frozen=True, slots=True)
class SapRuntime:
    """Placeholder Core runtime facade for future execution behavior."""
